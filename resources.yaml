apiVersion: v1
kind: Pod
metadata:
  name: resources
  labels:
    purpose: demo
    project: roboshop
spec:
  containers:
  - name: nginx
    image: nginx
    resources:
      requests:
        memory: "68mi"
        cpu: "100m"
      limits:
        memory: "128mi"
        cpu: "150m"
