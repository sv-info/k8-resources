apiVersion: v1
kind: Pod
metadata:
  name: env-configmap
  labels:
    purpose: demo
    project: roboshop
spec:
  containers:
    - name: nginx
      image: nginx
      envFrom:
        - configMapRef:
            name: pod-config